_..() {
	local opts
	opts=$(cd .. ; ls -d */ | sed 's|/./||')
	COMPREPLY=($(compgen -W "${opts}"))
}

_git_branches() {
	local opts
	opts=$(git branch --format "%(refname)" | sed 's|refs/heads/||')
	COMPREPLY=($(compgen -W "${opts} auto"))
}

_git_pr() {
	_git_checkout
}
